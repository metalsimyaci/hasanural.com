@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Ata.</h1>
<h2 class="text-danger">Beklenemyen bir şeyler oldu. Dış minnaklar iş başında.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>İstek Anahtarı:</strong> <code>@Model.RequestId</code>
    </p>
}
